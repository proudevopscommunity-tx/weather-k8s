apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: example
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: secretstore-sample
    kind: SecretStore
  target:
    name: app-secrets-asm # <name-of-k8s-secret-to-be-created>
    creationPolicy: Owner
      #  data:
      #  - secretKey: secret-key-to-be-managed
      #    remoteRef:
      #      key: provider-key
      #      version: provider-key-version
      #      property: provider-key-property
  dataFrom:
  - extract:
      key: weather-app-secrets  # <name-of-remote-key-in-the-provider>
